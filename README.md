

```text

[ 32byte 统一固定文件头 ]
  +-------------------------------------------------------------------------+
  | Magic(4) | Type(1) | Version(1) | HeadLen(2) | DataLen(8) | BuildId(16) |
  +-------------------------------------------------------------------------+

[ TLV 变长头部 ]
  元数据 (文件名，字符串池，基址...)

[ 二进制有序索引数据 ]
  由元数据中的字段决定结构和长度
  
  JS位置数组 / PG符号数组 / DWARF地址区间

```